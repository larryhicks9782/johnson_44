rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for AI Studio applet to allow both guest (unauthenticated) and authenticated actions
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
